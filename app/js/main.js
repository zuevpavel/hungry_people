$(document).ready(function(){
  $('.slider').slick({
    infinite: true,
    dots: true,
    arrows: false,
    // centerMode: true,
    // variableWidth: true,
  });
  $('.gallery__slider').slick({
    infinite: true,
    dots: true,
    arrows: false,
    slidesToShow: 4,
    // slidesToScroll: 1
    // centerMode: true,
    // variableWidth: true,
  });


  
});

